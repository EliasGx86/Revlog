// The app version shown in the account modal. Bump on every deploy batch:
// minor for features, patch for fixes; drop "-beta" at public launch.
// Keep package.json's "version" in sync.
export const APP_VERSION = "0.5.0-beta";
